version: '3.8'

services:
  chatbot:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: multi_agent_chatbot
    volumes:
      - .:/app
    environment:
      - PYTHONPATH=/app/src
    command: python -m src.graph
